# Copy to .streamlit/secrets.toml and fill in. Never commit the real file.
MONDAY_API_TOKEN = "your-monday-personal-api-token"
WORK_ORDERS_BOARD_ID = "1234567890"
DEALS_BOARD_ID = "0987654321"
GEMINI_API_KEY = "your-google-ai-studio-key"
GEMINI_MODEL = "gemini-2.5-flash"

# Optional: pin "today" so demo answers are reproducible.
# The supplied dataset runs to early 2026, so leaving this unset means
# "this quarter" correctly returns nothing.
# TODAY_OVERRIDE = "2025-12-15"
